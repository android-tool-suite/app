# 更新日志

## 1.8.0（2026-09-08）

### 新增特性

- Runtime Contract 提升到 2.1.0，新增受权限、MIME、大小和用户手势约束的 `file.export`；插件先写入自身隔离 Blob，再通过系统“另存为”界面导出，宿主不接受任意路径。
- 新增独立后台任务示例、任务运行／历史 ADB 命令和 Runtime 生命周期回归脚本，覆盖 Host/WebView、旋转、前后台、错误外壳、权限撤销、超时、重试及 Provider 重连。

### 优化与修复

- 补充 `system.logs.search` 可选时间范围与 Provider 回执的契约说明；不改变网络／日志授权范围。
- 网络 Capability 按 HTTP token 语法接收下划线请求头，修复米游社 `x-rpc-device_id` 等合法字段被拒绝的问题；保留敏感 Header scope 与危险字段限制，并拒绝控制字符注入。
- 修复 Dataset 分块读取未应用 offset 导致大文件重复读取文件头的问题，避免插件 ZIP 目录解析和 JSON 解析失败；增加跨块读取设备回归测试。
- Debug 新增归档检查、显式选择 Dataset 恢复与数据状态查询接口，复用正式归档校验和暂存恢复流程，诊断仅返回元数据。
- 移除 WebView 创建前固定的一秒等待，保留首帧加载外壳；Debug 日志增加页面就绪耗时，便于区分页面启动与业务数据加载。
- 普通插件可把自提供 Worker Capability 用作主页组件数据源；包内自调用不显示无意义权限，但 Worker 的网络、文件、调度等下游调用仍以插件自身授权检查。
- `network.request` 只在 scope 显式声明后允许标准 `Authorization` 或 `Cookie` 请求头；Host、代理与连接级请求头仍始终禁止，声明会进入权限指纹并显示在管理页。
- 插件 SDK 2.0.0 移除已退出的 API1 Tool、Host、主页组件和 Migration Bridge 接口；可信 Provider 可直接使用共享 Shizuku 客户端，取消待发布的 `readSystemLog` 平台桥，旧桥仅按需兼容。Runtime Contract 提供通用 `system.logs` 权限定义，普通插件不能获得完整日志或通用 Shell。
- 数据备份包含已安装 format v3 原始插件包，可恢复未发布的本地项目；与本地导入共用校验，失败回滚包版本，新装插件保持停用并重新检查权限/完全信任。
- 宿主停止装载或安装旧 `plugin.apk`；历史归档仍可识别，但不会重新执行其中的 API1 代码。
- Scheduler 的触发、约束、撤权、重试与租约决策抽为可测试策略，并补齐持久历史、并发租约和宿主生命周期 instrumentation 用例。
- Debug 文件导入与导出选择器在权限撤销、会话关闭和 Activity 销毁时会取消，不留下继续运行的宿主操作。

### 兼容性

- versionCode 提升到 24，为迁移后的 format v3 插件提供 `file.export`、自提供 Worker 主页能力和通用受限系统日志 Provider；插件 SDK 提升到 2.0.0。

## 1.7.0（2026-08-28）

### 新增特性

- 交付 Runtime v2 format v3：严格插件清单、确定性完整性清单、原子 generation 安装／回滚、版本化 RPC、TypeScript SDK 与 Capability Router。
- UI contribution 统一使用 `ui/*.json` 声明式文档；Host 组件树与隔离 WebView 是两种 renderer，共用设计 token、状态外壳、权限与生命周期，旧 `runtime.ui.type=web` 只保留读取兼容。
- 新增按插件管理的 Capability 权限：基础会话与私有存储自动允许，敏感能力默认待决定；授权绑定 scope 指纹，扩大范围后重新确认，撤销会阻止新调用、取消在途调用并停止相关后台任务。
- 普通插件可通过必需的 JavaScript Worker 提供自定义 Capability；Worker 的下游调用以提供者插件身份重新授权，不获得宿主身份。
- 新增 WorkManager 持久调度、API 24+ provider-task 与 API 26+ JavaScriptSandbox worker；任务具备约束、超时、重试、并发租约和持久历史。
- 新增宿主管理的 KV／blob、Keystore AES-GCM SecretStore、Dataset chunk API 与 staging generation 原子切换，并接入统一 `.atsbackup` v3 数据管理。
- 提供插件 CLI、契约生成、WebView／Host UI 与 Worker Capability 示例及 ADB 调试入口；Runtime Contract 首版为 `2.0.0`，插件 SDK 保持 `1.4.0`。

### 架构调整

- 运行时实现归并到 `plugin/runtime` 并改用职责类名；Web SDK、CLI 和示例分别整理到 `web-sdk`、`tools/plugin` 与 `examples/plugins`，不再把 V2 工作代号写入文件名或包名。
- Shizuku 授权及 Native Provider 源码迁移到独立 `plugin-shizuku-auth` 仓库，主体只保留平台生命周期桥和公开 SDK/契约。
- 插件 SDK 的 Native Provider API 从工作代号包 `.plugin.v2` 迁移到稳定包 `.plugin.runtime`；本次只整理源码命名，不改变 SDK 版本，Provider 插件随当前源码重新编译。
- 普通包禁止声明或夹带 Native Provider；必须以宿主身份与系统交互的插件使用签名的 `trusted-provider`，但仍可同时贡献 UI、主页组件、Worker 与普通 Tool 功能，管理页明确显示完全信任边界。
- `shizuku_auth` 从内置插件改为单个独立 `1.0.0 (1)` 全信任 V3 包：同包注册 `shizuku.control` 与 `accessibility.manage`，并用声明式 UI 查看连接、请求系统授权和连接 UserService；宿主只保留 Android manifest、UserService 与 Binder 生命周期要求的最小 bridge。
- API1 与 Runtime v2 插件使用统一迭代依赖装载；旧备份中的内置 `shizuku_auth` 启用状态可映射到同 ID 外部包。
- 旧版插件升级为同 ID format v3 包时，宿主会先导出并校验兼容 Dataset，再切换插件记录；失败恢复旧插件，旧偏好数据继续保留以便回滚。

### 安全与兼容性

- WebView 禁止远程导航、任意网络、文件访问、Cookie 与 DOM Storage，使用精确来源消息监听和 CSP；普通插件未声明或未授权的能力调用会在 Provider 执行前被拒绝。
- `trusted-provider` 必须通过 publisher 信任根签名验证；API1 与全信任 Provider 的原生代码不受普通插件权限开关约束。
- 权限页面只列出用户可以决定的权限：插件私有数据空间不再作为不可关闭的“权限”展示，完全信任插件自身也不再显示或接受权限开关。
- Shizuku 的 Binder、授权及系统服务连接变化会通知全部插件和主页组件，冷启动停留在主页时也能自动刷新最终状态；系统服务意外退出后会自动重新连接。
- WebView renderer 新增与 Compose 对齐的字体层级 token，Host 声明式 renderer 新增受限共享图标节点；Shizuku 工具页恢复状态图标、标题说明和并排操作的统一卡片结构。
- 主页小部件移除重复的“已就绪”标签，异常时才显示“需处理”；全信任 Provider 使用绿色强调背景，普通插件保留原有浅色背景，Shizuku 顶部说明改回通用中性提示色。
- Debug ADB 导入支持显式同版本开发替换，便于不占用正式版本号地复核 UI；正式安装入口仍拒绝同 `versionCode` 异包，替换过程不删除插件 Dataset。
- 最低 Android 版本保持 API 24；JavaScript worker 在 API 26 与 WebView 能力双重检查后启用，不用隐藏 WebView 模拟后台执行。
- 宿主 versionCode 仅从 22 提升到 23；Phigros 与抽卡分析继续保留 API1 兼容实现，本版本不提前迁移其业务 UI。
- API1 与 Migration Bridge 在剩余插件完成迁移并通过迁移、恢复、业务与降级测试前继续保留，只接受迁移与兼容修复；退出不附加版本数量或日历时间要求。

## 1.6.1（2026-08-21）

### 新增特性

- 正式版启用统一 `.atsbackup` v3 数据管理：应用设置、插件启用状态、各插件包和插件 Dataset 可以在一个包中独立选择并恢复。
- 导出项支持“不导出、明文、密码加密”三态；导入依据目标数据状态和插件声明提供跳过、导入、替换或合并，插件管理页同时提供单插件导入、导出和删除。
- 插件 SDK `1.3.1` 正式发布 `LegacyDataBridge` 的 Dataset 恢复、删除、已有数据探测和恢复模式契约。

### 优化

- 导入与导出采用一致的分组底部面板、连续描边容器和完整浅色／深色 surface 色阶，设置页的备份与迁移入口与其他设置项保持一致。
- 宿主设置、插件状态和插件包在写入前统一预检，并以迁移事务应用或回滚；插件数据在整包认证后按依赖顺序交由插件恢复。

### 安全与兼容性

- 明文区与密码区物理分离，密码区使用 PBKDF2-HMAC-SHA256 与 AES-256-GCM；敏感数据默认加密，显式选择明文时给出后果提示。
- 继续只读兼容 `.atsbackup` v2 与旧宿主迁移包；应用版本号提升到 22，本版本是 Bridge 预发布线完成真机往返验证后的首个正式版本。

## 1.6.0-bridge.5（2026-08-21）

### 修复

- 补全应用自带浅色与深色主题的 Material 3 容器、轮廓及其前景色角色，导入与导出底部面板不再回退到默认蓝紫色板。
- 导入与导出的应用／插件展开项改为一个连续的描边分组，Dataset 行在同一容器内以浅色底和分隔线呈现，与交互预览保持一致。

### 兼容性

- 应用版本号提升到 21；插件 SDK 更新为 `1.3.0-bridge.5`，共享设计系统明确提供完整的 surface container 色阶。

## 1.6.0-bridge.4（2026-08-21）

### 新增特性

- 统一数据包把 Android Tool Suite 数据拆分为应用设置、插件启用状态和每个外部插件的独立插件包，三类内容都可单独选择；插件包不再以一个不可拆分的宿主迁移快照导出。
- 导出选择为每个项目提供“不导出、明文、加密”三态；导入则依据本机状态提供“跳过、导入”或“跳过、替换、合并”，并明确标记已有数据和不支持合并的项目。
- 插件 SDK `1.3.0-bridge.4` 为 `LegacyDataBridge` 增加现有数据探测、恢复模式能力声明和显式恢复模式参数，由插件实现各 Dataset 的替换或合并语义。

### 优化

- 导入与导出统一为接近全高的分组底部面板；设置页的备份与迁移入口恢复为普通设置行，并移除未实现的账号保护、Debug 预览及无操作价值的辅助说明。
- 应用设置、插件启用状态和所选插件包在写入前完整解析与预检，并在同一宿主迁移事务中应用或回滚；插件数据仍在整包认证与暂存完成后按依赖顺序交给对应插件。

### 兼容性

- 应用版本号提升到 20；本版本的数据包结构直接采用拆分后的项目类型，不承诺与未正式发布的旧 v3 草案兼容。
- 统一数据管理入口和插件级数据操作不再受 Debug 构建限制，Debug 与 Release 使用相同的数据包协议和界面。

## 1.6.0-bridge.3（2026-08-20）

### 新增特性

- Debug 数据管理改用统一 `.atsbackup` v3：同一文件可同时承载应用设置、插件包、启用状态与按插件选择的 Dataset，并继续兼容 Bridge v2 和旧版宿主迁移包导入。
- 导出界面改为接近全高的分组选择面板，收起时汇总各应用／插件的已选、明文和加密数量，展开后为每个项目同时显示明文与密码保护选项；普通数据可加密，敏感数据允许在明确风险提示和独立确认后显式明文导出，并为未来账号绑定保护保留稳定格式类型。
- 插件管理页增加单插件数据导入、导出与删除入口；全局删除会按 Dataset 依赖展开选择并以反向依赖顺序执行。
- 插件 SDK `1.3.0-bridge.3` 为 `LegacyDataBridge` 增加默认关闭的 Dataset 删除能力，保持旧插件二进制兼容。

### 安全与可靠性

- v3 将明文与密码保护项目写入物理分离的数据区，各区独立记录大小和 SHA-256；恢复前先在私有缓存中完成所选区校验。
- 密码区继续使用 PBKDF2-HMAC-SHA256 与 AES-256-GCM；账号绑定保护在密钥服务落地前明确拒绝读写，不以占位逻辑伪装加密。

### 兼容性

- 应用版本号提升到 19；本版本仍只投放 Debug 渠道。统一包可在同一次恢复中先安装当前缺失的插件、刷新其 Bridge，再写入用户所选 Dataset。

## 1.6.0-bridge.2（2026-08-14）

### 新增特性

- Debug Migration Bridge 新增 `.atsbackup` v2 导入：检查来源、加密状态、Dataset 格式与插件支持情况，支持依赖联动选择和密码恢复。
- 插件 SDK `1.3.0-bridge.2` 为临时 `LegacyDataBridge` 增加默认关闭的恢复能力；旧的只读 Bridge 插件继续保持二进制兼容。

### 安全与可靠性

- 导入时先把所选 Dataset 解密到应用私有缓存，完成整包认证与完整性校验后才调用插件恢复，错误密码或损坏包不会提前改写插件数据。
- 恢复结束后删除明文暂存文件并清零内存中的密码；Dataset 按依赖顺序应用，插件负责事务写入、原子文件切换和写后校验。

### 兼容性

- 应用版本号提升到 18；本次导入只恢复 API1 Debug 安装的旧存储，不写入 Runtime v2 数据代管目录。

## 1.6.0-bridge.1（2026-08-14）

### 新增特性

- Debug 预发布新增只读 Migration Bridge：扫描所有已安装插件声明的 Dataset（包括已停用插件），支持自定义选择、依赖联动、敏感数据强制加密，并流式导出 `.atsbackup` v2。
- 插件 SDK `1.3.0-bridge.1` 增加临时 `LegacyDataBridge` 契约，旧插件通过默认方法保持二进制兼容。

### 安全与限制

- Bridge 不写入、不删除现有插件数据，也不包含 Runtime v2 恢复、双 APK Sandbox、权限代理或数据代管实现。
- Android Debug 包使用独立应用 ID，只能读取该 Debug 安装自身的数据；迁移正式版私有数据仍需后续同包名 Release Bridge。

### 兼容性

- 应用版本号提升到 17；本版本只投放 Debug 渠道，正式迁移版本将在 Bridge 格式和真实数据验收完成后另行发布。

## 1.5.1（2026-08-12）

### 优化

- 主分页改为直接以 `PagerState` 驱动页面、底栏选中态和滑动指示器。点击底栏会立即启动分页动画，不再先经过一层中间导航状态；相邻页沿用 AndroidX Pager 动画，跨越三页以上时连续滚过完整路径，避免官方长距离预跳在五页主导航中表现为动画缺段。
- 保留手势翻页与底栏指示器跟随，分页停稳后才同步宿主返回栈语义；快速连续点击会取消前一段动画，并从当前可见位置转向新目标。

### 修复

- 从仓库安装、更新或本地导入插件完成后停留在原页面，通过 Snackbar 显示结果，不再自动打开插件详情页；插件重载、启用状态和失败回滚逻辑保持不变。

### 文档

- 新增性能优化路线，记录 Debug／Release 差异、真机基准用例、Macrobenchmark 指标、可能的主线程与 Compose 热点，以及后续优化的验收顺序。本版本不实施这些性能改造。

## 1.5.0（2026-08-12）

### 新增特性

- 主导航扩展为主页、工具、管理、仓库和设置五个入口，并新增关于页面，集中提供主题、配色、更新、迁移和版本信息。
- 五个一级分区支持左右滑动切换，与点击底栏等价。
- 每个外部插件可单独关闭更新检查。关闭后不再计入更新角标、更新弹窗与「全部更新」，仓库里仍可手动选版本安装。
- 已安装插件在仓库中恢复版本选择：可切到其他已发布版本或降级，每行标出这次跳转是升级、切换构建、可降级还是被数据兼容性拦下。
- 插件管理改为默认收起的状态摘要卡，展开后可控制显示位置、更新检查，并导出或删除插件；仓库改为已安装／可安装两段式布局，插件详情统一使用标准顶栏。

### 优化

- 一级页面移除重复的顶部标题栏，仓库刷新入口并入内容分段；插件详情与关于子页仍保留返回顶栏。
- 左右连续滑动时，底栏和宽屏侧栏的选中加深直接跟随 Pager 当前页，宿主导航状态仍在页面停稳后提交。
- 底栏选中背景改为跟随手指和 Pager 动画连续滑动，不再在跨过页面中点时突然跳到下一项。
- 点击不相邻的底栏入口也会从当前页立即开始连续翻页，内容和底部指示器一起走完整路径，不再预跳到目标页附近。
- 手势翻页阈值从默认的半页降到 30%，短一些但方向明确的左右划动也能稳定切页。
- 启动阶段接入系统 SplashScreen，在首屏就绪后用淡出和轻微缩放过渡到主界面。
- 插件 SDK 提升到 1.2.0，新增统一间距、圆角、语义色、设置行、顶栏及加载／错误／提示组件；默认使用应用自带配色，动态取色改为可选。
- 自动更新无结果或失败时保持静默，手动检查改用 Snackbar 反馈，有更新时集中显示更新弹窗和仓库角标。
- 深色模式窗口主题、宽屏内容边界与状态栏 inset 统一处理，平台对话框迁移为 Compose 对话框。
- 状态栏与导航栏图标明暗跟随应用实际主题，把主题钉死为浅色或深色时不再出现与系统深色模式不一致导致的图标看不见。
- 列表左沿统一使用圆角方块图标底座，插件与仓库条目不再因图标本身的视觉重量而参差；仓库条目副标题直接给出版本走向。
- 浅色档的 `primaryContainer` 与 `secondaryContainer` 降低亮度和饱和度：前者铺满主页摘要卡，后者出现在每个图标底座上，原先的亮青在浅色背景下过于刺眼。
- 主页小部件保持长按出菜单调整尺寸或移除、长按拖动排序；隐藏项的恢复入口放在「主页小部件」分段标题右侧，跟着有隐藏项时才出现。
- 工具页长按整卡出菜单，可从工具页隐藏、停用插件或跳到管理；隐藏数量与恢复入口写在列表末尾。
- 设置项去掉装饰性图标，可执行项标题改用主色区分，有下拉可选的行补 ▾ 提示。关于页合并为单组，版本号行写明长按可复制完整版本串。

### 修复

- 仓库安装或更新插件后立即显示下载进度，不再需要切出页面再返回；应用更新也显示同样的准备状态与百分比。
- 主题、配色和调试仓库渠道的下拉菜单统一右侧锚点、宽度、选中勾选与主色样式。
- 手动刷新仓库、检查更新的开始、成功和失败反馈统一使用 Snackbar，避免操作后没有可见响应。
- 浅色警告、错误与成功容器改用对应的容器前景色，修复浅底白字导致的低对比度。
- 插件状态变化和宿主对话框在当前页面立即刷新，不再依赖离开后重新进入页面。
- 主页小部件的点按与长按不再失效。小部件内容是插件提供的 View，会把触摸事件吃掉，外层收不到手势；现在由一层只拦不消费的覆盖层转交给手势判定。
- 管理页插件卡整行都能展开。内边距原先加在外层容器上，卡片左右各有一条点不到的死区。已停用的插件也能展开，导出与删除就在展开区里。
- 在仓库里删除插件后停留在仓库。此前重载插件会把当前分区重置到工具页，看起来像删除没有生效。
- 管理页恢复插件的导出与删除入口。

### 性能

- 下载进度改为卡片级可观察状态，高频进度回调不再触发整个宿主界面重组。
- 页面滑动只在停稳后回写导航状态；点击跨越多个底栏分区时使用固定时长动画，避免距离越长过渡越慢。
- Pager 仅缓存当前页两侧的相邻页面，连续来回滑动时不再反复销毁和重建复杂列表。
- 外部插件在主界面出现前完成加载；运行期只缓存当前页两侧的相邻页，页面停稳时静默同步返回栈状态，不再让全部页面跟着重组。
- 底栏滑动指示器在图层阶段直接更新 GPU 平移矩阵，不再因每帧位置变化重新组合和布局底栏。
- Android 15 及以上从按下时开启窗口触摸升帧，并按屏幕支持的最高刷新率向 Compose 根视图和绘制视图投票；覆盖 Pager 滑动与惯性回弹后恢复默认，静止时仍可降到低刷新率。
- 去掉每次宿主状态失效时同步遍历所有列表位置的工作，列表位置继续由各页面自己的滚动状态流保存。
- 缓存插件描述符与插件类型查询，工具页和管理页绘制卡片时不再反复读取、解析整个插件清单或重新实例化内置插件。
- 启停插件时不再自动打开插件详情页，减少插件重载后的额外 View 创建。

### 工具链修复

- 本地 debug 构建可通过 `local.properties` 的 `atsDebugSigningProperties` 指向仓库外的签名属性文件，使用与 CI 相同的专用签名，不再需要手工导出四个环境变量；密钥、口令与本机路径都留在仓库外。构建会打印所用签名来源，未配置时回落到默认 keystore 并说明后果——包无法覆盖安装 CI 版本，且装上后应用内更新会被系统安装器拒绝。
- `tools/adb-debug.ps1` 按 UTF-8 解码 adb 输出。此前在中文 Windows 上，响应里的中文插件名会被按系统代码页解坏并吞掉紧随的引号，导致 `status`、`list-plugins`、`import-plugin` 等命令全部解析失败。

### 兼容性

- 未声明数据兼容性的旧插件和安装记录统一按数据格式 `v0` 判断；发布元数据允许显式声明可读 `v0`，不再把旧数据一概标为未知并阻止覆盖。
- 保留原有 `SuiteTheme(Boolean, Boolean, content)` 入口；外部插件可逐步迁移到进程内可观察的主题偏好与 SDK 1.2.0 组件。
- `SuiteColors` 标记为废弃但保留，避免按 1.1.0 编译的外部插件在运行时找不到字段；请改用成对深浅的 `SuiteSemantic.current`。
- 新增 `SuiteTheming.isDark`，供插件为自己的领域配色选择深浅档位。稀有度、难度这类游戏配色不进 SDK，由各插件自行维护。
- SDK 1.2.0 的成功、警告、危险和信息语义色补齐 `on*Container` 前景色；既有字段与主题入口保持不变。

## 1.4.0（2026-08-11）

### 新增特性

- 应用内插件仓库接入签名历史目录，可在正式仓库和调试仓库中选择具体插件版本或 Debug 提交。
- 插件发布元数据新增数据格式兼容声明，宿主记录已安装版本可能写入的数据格式，并区分安装、升级、切换构建和降级。

### 安全

- 仅在目标历史版本能够读取当前数据格式时允许降级，并在执行前再次确认；旧版缺少声明时按 `v0` 判断，明确不兼容时阻止覆盖安装。
- 历史版本仍需通过目录签名、资产大小、SHA-256、插件清单、宿主兼容性、依赖和代码预检，失败时继续事务式恢复旧插件代码与安装记录。

### 升级说明

- 旧 Release 没有数据兼容声明时按 `v0` 处理；从写入更高格式的版本降级到旧版仍会被阻止，除非目标版本明确声明相应可读范围。

## 1.3.1（2026-08-07）

### 新增特性

- Debug 应用使用独立包名与带角标图标，可与正式版同时安装，并通过稳定专用签名接收滚动调试更新。
- 插件管理新增宿主迁移包，可在 Debug 与 Release 之间迁移插件包、启用状态、界面布局和仓库选择。

### 优化

- 正式版与调试版分别检查自身更新通道；调试版按提交 SHA 识别相同版本号的新构建。
- 跨仓库索引通知改用短时 GitHub App installation token，不再依赖个人访问令牌。

### 安全

- 迁移包经过路径、大小、摘要、兼容性、依赖和代码预检，并在失败时回滚；账号凭据、插件业务数据和 Android Keystore 密钥不会导出。

## 1.3.0（2026-08-07）

### 新增特性

- 插件仓库新增正式与调试两个通道；Release 应用也可以显式选择调试仓库，并可从仓库页直接导入本地 `.atsplugin`。
- 主体 CI 在 `main` 构建成功后发布滚动 `debug` 预发布，正式标签仍发布签名 Release APK。

### 优化

- 正式应用更新与插件仓库选择解耦：宿主始终从正式索引检查自身更新，插件列表按用户选择读取正式或调试索引。
- 调试插件按已签名索引中的 SHA-256 判断新构建，允许相同版本号的后续 CI 构建更新，也允许在调试版和正式版之间切换。
- 组件发布完成后以事件通知插件索引重建，不再依赖高频定时轮询。

### 安全

- 正式、调试索引都继续验证 ECDSA 签名；本地导入仍明确标记为未经仓库验证，不与远程调试构建混淆。

## 1.2.0（2026-07-24）

### 新增特性

- 新增基于签名 GitHub Pages 索引的应用更新检查和官方插件仓库，支持自动检查、手动刷新、插件安装与一键更新。
- 新增正式签名 Release APK、组件 CI 和语义化标签触发的 GitHub Release 发布流程。

### 优化

- 插件包格式升级为向后兼容的 v2，记录整数版本号、最低宿主版本和构建所用插件 SDK。
- 插件更新采用带临时文件、备份和失败恢复的事务式替换，并保留原有启用状态。
- 插件 SDK 提升到 1.1.0，为宿主提供可靠的插件版本与兼容性元数据。

### 安全

- 应用和插件下载均校验签名索引、文件大小与 SHA-256；应用更新额外校验包名、版本号和签名证书。
- 手动导入的插件继续受支持，但会与官方仓库验证的插件明确区分来源。

## 1.1.8（2026-07-18）

### 问题修复

- 修复插件管理的界面管理页面在切换工具页或主页显示状态后未实时刷新、需要退出重进才能显示最新开关状态的问题。

## 1.1.7（2026-07-13）

### 优化

- 参考 HyperOS 桌面图标风格，将应用图标重绘为深青纯色底、白色文件卡片与薄荷绿模块组成的扁平符号，移除 3D 材质、渐变、阴影和多层边框。
- 分离自适应图标前景与普通、圆形图标资源，自适应前景额外缩小并使用对比色分隔文件与模块，避免厂商启动器二次缩放时裁切内容或合并相邻轮廓。

## 1.1.6（2026-07-13）

### 优化

- 应用启动、Shizuku Binder 就绪或授权完成后，在已有授权的前提下自动连接 UserService，无需再进入工具页手动连接。
- 未启动 Shizuku 或尚未授权时仍保持原有提示与手动授权流程，不会自动发起权限请求。

## 1.1.5（2026-07-13）

### 新增特性

- Phigros 本地令牌新增“设置”入口，可修改令牌备注而不改动加密密文、服务器、历史记录和当前选择。
- Phigros 令牌卡片显示服务器、本地加密状态和最近使用时间，不再显示无意义的固定掩码。

### 优化

- 插件版本号改为仅在 Gradle 中维护；运行时描述、插件清单和 Phigros 图片署名在构建时自动读取或注入版本号。

### 问题修复

- 修复 Shizuku 授权或 UserService 连接完成后页面状态不自动刷新的问题；详情页和主页组件会持续观察实际 Binder 状态并及时重组。

## 1.1.4（2026-07-13）

### 优化

- 应用包名由原型阶段的 `com.example.shizukuaccessibilitygrant` 调整为与应用定位一致的 `com.androidtoolsuite.app`，源码、AIDL、调试命令、插件入口和文档同步迁移。
- 重做普通、圆形和 Android 8.0 以上自适应应用图标，保持与应用界面一致的深青、薄荷绿和珊瑚粉配色。

### 升级说明

- 包名变化后 Android 会将 `1.1.4` 视为新应用，旧包数据不会由系统自动继承；请在确认新应用数据和插件可用后再卸载旧应用。

## 1.1.3（2026-07-13）

### 新增特性

- 新增“插件管理 → 界面管理”，每个插件集中提供“工具页”和“主页”两个显示开关。
- 主页和工具页新增长按拖动落点虚影；拖动期间只预览位置，松手后才保存排序。
- 外部插件加载失败时显示兼容性提示，便于重新导入与当前宿主匹配的完整插件包。

### 优化

- 主页和工具页复用相同的拖动状态、蓝色虚影、圆角和边框，统一交互与视觉风格。
- 主页小部件改为统一网格布局，支持不同宽高组件稳定换位，并保留长按尺寸菜单。
- 插件详情页移除重复标题和顶部空白，使用浮动返回按钮保留正确的页面来源。
- 插件管理按插件聚合界面显示设置，避免同一插件因工具和小部件重复出现。
- 插件只允许通过包含 `manifest.json`、`plugin.apk` 和可执行入口的完整 `.atsplugin` 包导入。

### 问题修复

- 修复底部导航切换后选中状态不更新的问题。
- 修复从主页打开插件后返回到工具页而不是主页的问题。
- 修复外部插件代码加载失败时仍被调试状态误报为 active 的问题。
- 修复主页混合尺寸组件换位时拖动中断、连续跳位、拖向空白处回弹以及圆角外出现矩形阴影的问题。
- 修复工具页编辑状态与普通状态显示不一致的问题，移除单独编辑界面和上下箭头排序。
- 移除无法可靠限制同进程插件能力的声明式权限系统，改为明确提示仅安装可信插件。
- 移除只导入 JSON 插件说明的入口，避免出现没有可执行代码的伪插件。
