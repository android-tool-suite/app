# 更新日志

## 1.5.0（2026-08-11）

### 新增特性

- 主导航扩展为主页、工具、管理、仓库和设置五个入口，并新增关于页面，集中提供主题、配色、更新、迁移和版本信息。
- 五个一级分区支持左右滑动切换，与点击底栏等价。
- 每个外部插件可单独关闭更新检查。关闭后不再计入更新角标、更新弹窗与「全部更新」，仓库里仍可手动选版本安装。
- 已安装插件在仓库中恢复版本选择：可切到其他已发布版本或降级，每行标出这次跳转是升级、切换构建、可降级还是被数据兼容性拦下。
- 插件管理改为默认收起的状态摘要卡，展开后可控制显示位置、更新检查，并导出或删除插件；仓库改为已安装／可安装两段式布局，插件详情统一使用标准顶栏。

### 优化

- 插件 SDK 提升到 1.2.0，新增统一间距、圆角、语义色、设置行、顶栏及加载／错误／提示组件；默认使用应用自带配色，动态取色改为可选。
- 自动更新无结果或失败时保持静默，手动检查改用 Snackbar 反馈，有更新时集中显示更新弹窗和仓库角标。
- 深色模式窗口主题、宽屏内容边界与状态栏 inset 统一处理，平台对话框迁移为 Compose 对话框。
- 状态栏与导航栏图标明暗跟随应用实际主题，把主题钉死为浅色或深色时不再出现与系统深色模式不一致导致的图标看不见。
- 列表左沿统一使用圆角方块图标底座，插件与仓库条目不再因图标本身的视觉重量而参差；仓库条目副标题直接给出版本走向。
- 浅色档的 `primaryContainer` 与 `secondaryContainer` 降低亮度和饱和度：前者铺满主页摘要卡，后者出现在每个图标底座上，原先的亮青在浅色背景下过于刺眼。
- 主页小部件保持长按出菜单调整尺寸或移除、长按拖动排序；隐藏项的恢复入口放在「主页小部件」分段标题右侧，跟着有隐藏项时才出现。
- 工具页长按整卡出菜单，可从工具页隐藏、停用插件或跳到管理；隐藏数量与恢复入口写在列表末尾。
- 设置项去掉装饰性图标，可执行项标题改用主色区分，有下拉可选的行补 ▾ 提示。关于页合并为单组，版本号行写明长按可复制完整版本串。

### 修复

- 主页小部件的点按与长按不再失效。小部件内容是插件提供的 View，会把触摸事件吃掉，外层收不到手势；现在由一层只拦不消费的覆盖层转交给手势判定。
- 管理页插件卡整行都能展开。内边距原先加在外层容器上，卡片左右各有一条点不到的死区。已停用的插件也能展开，导出与删除就在展开区里。
- 在仓库里删除插件后停留在仓库。此前重载插件会把当前分区重置到工具页，看起来像删除没有生效。
- 管理页恢复插件的导出与删除入口。

### 工具链修复

- 本地 debug 构建可通过 `local.properties` 的 `atsDebugSigningProperties` 指向仓库外的签名属性文件，使用与 CI 相同的专用签名，不再需要手工导出四个环境变量；密钥、口令与本机路径都留在仓库外。构建会打印所用签名来源，未配置时回落到默认 keystore 并说明后果——包无法覆盖安装 CI 版本，且装上后应用内更新会被系统安装器拒绝。
- `tools/adb-debug.ps1` 按 UTF-8 解码 adb 输出。此前在中文 Windows 上，响应里的中文插件名会被按系统代码页解坏并吞掉紧随的引号，导致 `status`、`list-plugins`、`import-plugin` 等命令全部解析失败。

### 兼容性

- 保留原有 `SuiteTheme(Boolean, Boolean, content)` 入口；外部插件可逐步迁移到进程内可观察的主题偏好与 SDK 1.2.0 组件。
- `SuiteColors` 标记为废弃但保留，避免按 1.1.0 编译的外部插件在运行时找不到字段；请改用成对深浅的 `SuiteSemantic.current`。
- 新增 `SuiteTheming.isDark`，供插件为自己的领域配色选择深浅档位。稀有度、难度这类游戏配色不进 SDK，由各插件自行维护。

## 1.4.0（2026-08-11）

### 新增特性

- 应用内插件仓库接入签名历史目录，可在正式仓库和调试仓库中选择具体插件版本或 Debug 提交。
- 插件发布元数据新增数据格式兼容声明，宿主记录已安装版本可能写入的数据格式，并区分安装、升级、切换构建和降级。

### 安全

- 仅在目标历史版本明确声明能够读取当前数据格式时允许降级，并在执行前再次确认；明确不兼容或缺少声明时阻止覆盖安装。
- 历史版本仍需通过目录签名、资产大小、SHA-256、插件清单、宿主兼容性、依赖和代码预检，失败时继续事务式恢复旧插件代码与安装记录。

### 升级说明

- 旧 Release 没有数据兼容声明时仍可首次安装，但不能从已有较新版本直接降级；需等待插件发布带声明的版本或先使用插件自身功能妥善迁移业务数据。

## 1.3.1（2026-08-07）

### 新增特性

- Debug 应用使用独立包名与带角标图标，可与正式版同时安装，并通过稳定专用签名接收滚动调试更新。
- 插件管理新增宿主迁移包，可在 Debug 与 Release 之间迁移插件包、启用状态、界面布局和仓库选择。

### 优化

- 正式版与调试版分别检查自身更新通道；调试版按提交 SHA 识别相同版本号的新构建。
- 跨仓库索引通知改用短时 GitHub App installation token，不再依赖个人访问令牌。

### 安全

- 迁移包经过路径、大小、摘要、兼容性、依赖和代码预检，并在失败时回滚；账号凭据、插件业务数据和 Android Keystore 密钥不会导出。

## 1.3.0（2026-08-07）

### 新增特性

- 插件仓库新增正式与调试两个通道；Release 应用也可以显式选择调试仓库，并可从仓库页直接导入本地 `.atsplugin`。
- 主体 CI 在 `main` 构建成功后发布滚动 `debug` 预发布，正式标签仍发布签名 Release APK。

### 优化

- 正式应用更新与插件仓库选择解耦：宿主始终从正式索引检查自身更新，插件列表按用户选择读取正式或调试索引。
- 调试插件按已签名索引中的 SHA-256 判断新构建，允许相同版本号的后续 CI 构建更新，也允许在调试版和正式版之间切换。
- 组件发布完成后以事件通知插件索引重建，不再依赖高频定时轮询。

### 安全

- 正式、调试索引都继续验证 ECDSA 签名；本地导入仍明确标记为未经仓库验证，不与远程调试构建混淆。

## 1.2.0（2026-07-24）

### 新增特性

- 新增基于签名 GitHub Pages 索引的应用更新检查和官方插件仓库，支持自动检查、手动刷新、插件安装与一键更新。
- 新增正式签名 Release APK、组件 CI 和语义化标签触发的 GitHub Release 发布流程。

### 优化

- 插件包格式升级为向后兼容的 v2，记录整数版本号、最低宿主版本和构建所用插件 SDK。
- 插件更新采用带临时文件、备份和失败恢复的事务式替换，并保留原有启用状态。
- 插件 SDK 提升到 1.1.0，为宿主提供可靠的插件版本与兼容性元数据。

### 安全

- 应用和插件下载均校验签名索引、文件大小与 SHA-256；应用更新额外校验包名、版本号和签名证书。
- 手动导入的插件继续受支持，但会与官方仓库验证的插件明确区分来源。

## 1.1.8（2026-07-18）

### 问题修复

- 修复插件管理的界面管理页面在切换工具页或主页显示状态后未实时刷新、需要退出重进才能显示最新开关状态的问题。

## 1.1.7（2026-07-13）

### 优化

- 参考 HyperOS 桌面图标风格，将应用图标重绘为深青纯色底、白色文件卡片与薄荷绿模块组成的扁平符号，移除 3D 材质、渐变、阴影和多层边框。
- 分离自适应图标前景与普通、圆形图标资源，自适应前景额外缩小并使用对比色分隔文件与模块，避免厂商启动器二次缩放时裁切内容或合并相邻轮廓。

## 1.1.6（2026-07-13）

### 优化

- 应用启动、Shizuku Binder 就绪或授权完成后，在已有授权的前提下自动连接 UserService，无需再进入工具页手动连接。
- 未启动 Shizuku 或尚未授权时仍保持原有提示与手动授权流程，不会自动发起权限请求。

## 1.1.5（2026-07-13）

### 新增特性

- Phigros 本地令牌新增“设置”入口，可修改令牌备注而不改动加密密文、服务器、历史记录和当前选择。
- Phigros 令牌卡片显示服务器、本地加密状态和最近使用时间，不再显示无意义的固定掩码。

### 优化

- 插件版本号改为仅在 Gradle 中维护；运行时描述、插件清单和 Phigros 图片署名在构建时自动读取或注入版本号。

### 问题修复

- 修复 Shizuku 授权或 UserService 连接完成后页面状态不自动刷新的问题；详情页和主页组件会持续观察实际 Binder 状态并及时重组。

## 1.1.4（2026-07-13）

### 优化

- 应用包名由原型阶段的 `com.example.shizukuaccessibilitygrant` 调整为与应用定位一致的 `com.androidtoolsuite.app`，源码、AIDL、调试命令、插件入口和文档同步迁移。
- 重做普通、圆形和 Android 8.0 以上自适应应用图标，保持与应用界面一致的深青、薄荷绿和珊瑚粉配色。

### 升级说明

- 包名变化后 Android 会将 `1.1.4` 视为新应用，旧包数据不会由系统自动继承；请在确认新应用数据和插件可用后再卸载旧应用。

## 1.1.3（2026-07-13）

### 新增特性

- 新增“插件管理 → 界面管理”，每个插件集中提供“工具页”和“主页”两个显示开关。
- 主页和工具页新增长按拖动落点虚影；拖动期间只预览位置，松手后才保存排序。
- 外部插件加载失败时显示兼容性提示，便于重新导入与当前宿主匹配的完整插件包。

### 优化

- 主页和工具页复用相同的拖动状态、蓝色虚影、圆角和边框，统一交互与视觉风格。
- 主页小部件改为统一网格布局，支持不同宽高组件稳定换位，并保留长按尺寸菜单。
- 插件详情页移除重复标题和顶部空白，使用浮动返回按钮保留正确的页面来源。
- 插件管理按插件聚合界面显示设置，避免同一插件因工具和小部件重复出现。
- 插件只允许通过包含 `manifest.json`、`plugin.apk` 和可执行入口的完整 `.atsplugin` 包导入。

### 问题修复

- 修复底部导航切换后选中状态不更新的问题。
- 修复从主页打开插件后返回到工具页而不是主页的问题。
- 修复外部插件代码加载失败时仍被调试状态误报为 active 的问题。
- 修复主页混合尺寸组件换位时拖动中断、连续跳位、拖向空白处回弹以及圆角外出现矩形阴影的问题。
- 修复工具页编辑状态与普通状态显示不一致的问题，移除单独编辑界面和上下箭头排序。
- 移除无法可靠限制同进程插件能力的声明式权限系统，改为明确提示仅安装可信插件。
- 移除只导入 JSON 插件说明的入口，避免出现没有可执行代码的伪插件。
