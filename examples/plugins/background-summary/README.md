# 后台任务示例

这个普通 format v3 插件演示独立于无障碍授权的后台任务：

- `app-foreground` 与 60 分钟 `periodic` 触发；
- 手动运行、最近运行状态和触发来源；
- `forbid` 并发策略、超时、重试和电量／存储约束；
- 只供 ADB 回归调用的超时与可重试失败探针；
- Worker 只返回计数与完成时间，不把账号、令牌或原始业务载荷写入任务记录。

任务仍需要用户授予“后台运行”权限；撤销权限后 Host 会取消已登记任务和活动运行。
