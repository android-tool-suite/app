package com.androidtoolsuite.app.plugin.migration;

/** Stable top-level classification used by backup, restore, and cleanup UIs. */
public enum DatasetCategory {
    SETTINGS,
    DATA,
    SECRET,
    CACHE
}
